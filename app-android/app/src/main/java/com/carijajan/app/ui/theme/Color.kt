package com.carijajan.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─────────────────────────────────────────────────────────────────────────────
// CariJajan color palette
// Warna utama: oranye hangat (makanan street food) + hijau segar (segar/buka)
// ─────────────────────────────────────────────────────────────────────────────

// Primary — oranye amber (khas street food, hangat & mengundang)
val OrangeAmber     = Color(0xFFFF8C00)
val OrangeAmberDark = Color(0xFFE65100)
val OrangeAmberLight= Color(0xFFFFB74D)

// Secondary — hijau toska (indikator "buka")
val TealGreen       = Color(0xFF00897B)
val TealGreenDark   = Color(0xFF004D40)
val TealGreenLight  = Color(0xFF4DB6AC)

// Error
val ErrorRed        = Color(0xFFD32F2F)

// Background & surface (light)
val Cream           = Color(0xFFFFFBF0)   // latar agak warm
val SurfaceLight    = Color(0xFFFFFFFF)
val SurfaceVariant  = Color(0xFFF5F0E8)

// Background & surface (dark)
val DarkBackground  = Color(0xFF1A1208)   // sangat gelap warm
val DarkSurface     = Color(0xFF2A1E0E)
val DarkSurfaceVar  = Color(0xFF3D2E18)

// On-colors
val OnOrange        = Color(0xFFFFFFFF)
val OnTeal          = Color(0xFFFFFFFF)

// Status colors
val ColorOpen       = Color(0xFF2E7D32)   // hijau tua — lapak buka
val ColorClosed     = Color(0xFF616161)   // abu — lapak tutup
val ColorStale      = Color(0xFFE65100)   // oranye tua — data mungkin basi (>4 jam)

// Category badge colors (subtle pastel)
val CategoryCilok       = Color(0xFFFFF3E0)
val CategoryBatagor     = Color(0xFFE8F5E9)
val CategorySiomay      = Color(0xFFE3F2FD)
val CategoryGorengan    = Color(0xFFFFF9C4)
val CategoryMinuman     = Color(0xFFE8EAF6)
val CategoryMakananBerat= Color(0xFFFCE4EC)
val CategoryDessert     = Color(0xFFF3E5F5)
val CategoryLainnya     = Color(0xFFF5F5F5)
